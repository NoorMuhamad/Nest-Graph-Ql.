export enum UserRole {
  OWNER = 'owner',
  ADMIN = 'admin',
  STUFF = 'stuff',
}

export enum ComplaintStatus {
  PENDING = 'pending',
  INPROGRESS = 'inProgress',
  COMPLETE = 'complete',
}

export enum BillStatus {
  PAID = 'paid',
  UNPAID = 'unpaid',
}
