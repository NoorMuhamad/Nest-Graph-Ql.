export enum UserRole {
  OWNER = 'owner',
  ADMIN = 'admin',
  STAFF = 'staff',
}

export enum ComplaintStatus {
  PENDING = 'pending',
  INPROGRESS = 'inProgress',
  COMPLETE = 'complete',
}

export enum BillStatus {
  PAID = 'paid',
  UNPAID = 'unpaid',
}
